t = int(input())
for _ in range(t):
    n = int(input())
    a = list(map(int, input(). split()))
    total = 0
    ans = 10 ** 20
    for i in range(n):
        total += a[i]
        curr_ave = total // (i + 1)
        ans = min(ans, curr_ave)
        print(ans, end=" ")
    print()    
