class Solution:
    def transpose(self, matrix: List[List[int]]) -> List[List[int]]:
        new = []
        for column in range(len(matrix[0])):
            row = []
            for i in range(len(matrix)):
                row. append(matrix[i][column])
            new. append(row)
        return new
