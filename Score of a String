class Solution:
    def scoreOfString(self, s: str) -> int:
        i = 0
        sum = 0
        for i in range(len(s) - 1):
            final = abs(ord(s[i]) - ord(s[i + 1]))
            sum += final
        return sum
        
