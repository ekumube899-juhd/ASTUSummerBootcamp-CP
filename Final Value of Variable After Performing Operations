class Solution:
    def finalValueAfterOperations(self, operations: List[str]) -> int:
        x = 0
        for arr in operations:
            if "++" in arr:
                x += 1
            else:
                x -= 1
        return x  
